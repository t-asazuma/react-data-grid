import styled from 'styled-components'
import {BaseComponent} from "./BaseComponent";

const StyledTableWrapper = styled(BaseComponent)`
  width: 100%;
  height: 100%;
`

export default StyledTableWrapper
