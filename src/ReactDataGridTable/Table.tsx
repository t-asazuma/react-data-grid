import React, {FC, ReactNode} from 'react'
import styled from "styled-components";
import {BaseComponent} from "./BaseComponent";

const StyledTable = styled(BaseComponent)`
  display: flex;
  width: 100%;
  height: 100%;
  max-width: 100%;
  flex-direction: column;
`
export default StyledTable
